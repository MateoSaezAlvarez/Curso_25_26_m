import { extraerData } from "./myFunction"

//Pasandole un array de productos como parámetro y me devuelva el nombre del producto que tiene la máxima RAM
export const maxRam = (arrayProductos)=>{
    arrayProductos.map(extraerData).reduce((max,actual,)=>{
        maxRam.slice(-2)
    }, 0)
}